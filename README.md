># 머신러닝
>데이터에서 `규칙`을 학습하는 알고리즘(machine-learning)
``` 
데이터 전처리 → 모델 생성＆훈련 → 모델 성능 조절 → 모델 평가＆데이터 예측
```

## 데이터 분석
```angular2html
# 전체
테이블_변수.head()
테이블_변수.info()
테이블_변수.describe()

# 속성별
테이블_변수.value_count()

# 상관관계
corr_matrix = 테이블_변수.corr()    
corr_matrix['속성'].sort_values(ascending = False)    # 상관계수(속성＆나머지)
```

## 데이터 전처리
```
결측치
속성 상관관계 시각화??
라벨 인코딩
필요없는 속성 삭제

특성(+타겟) 선택, 추출
데이터 분할
데이터 스케일링

차원 축소
```

## 모델 생성＆훈련
```angular2html
모델 선택
```

## 모델 성능 조절
```angular2html
교차 검증
그리드 서치＆랜덤 서치(교차 검증 + 하이퍼파라미터 최적화)
```

## 모델 평가＆데이터 예측
```angular2html
지도 학습, 비지도 학습
```




